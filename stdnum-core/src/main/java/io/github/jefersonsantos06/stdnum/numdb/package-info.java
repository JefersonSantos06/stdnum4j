/**
 * Prefix database for hierarchical number formats (IBAN country registry,
 * ISBN ranges, ...): indentation-structured data files parsed lazily from
 * the classpath into an immutable range tree.
 */
package io.github.jefersonsantos06.stdnum.numdb;
