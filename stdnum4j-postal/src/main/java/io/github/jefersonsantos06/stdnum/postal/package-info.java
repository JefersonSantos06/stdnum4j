/**
 * Postal codes of every country and territory that has one, each a
 * {@link io.github.jefersonsantos06.stdnum.spi.StdNum} registered as
 * {@code <cc>.postal_code}, in the shape Google's address metadata — the data
 * behind libaddressinput — documents for it.
 */
package io.github.jefersonsantos06.stdnum.postal;
